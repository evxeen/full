{
  "name": "project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "b": "pnpm --filter @project/backend",
    "w": "pnpm --filter @project/webapp",
    "d": "pnpm -r --parallel dev",
    "types": "concurrently \"pnpm -- b types\" \"pnpm -- w types\"",
    "prettify": "pnpm -r --parallel prettify"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "eslint": "^9.20.1",
    "eslint-config-prettier": "^10.0.1",
    "eslint-config-react-app": "^7.0.1",
    "eslint-config-standard-with-typescript": "^43.0.1",
    "prettier": "^3.0.2",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.1.6"
  }
}
